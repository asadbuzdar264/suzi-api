services:
  - type: web
    name: suzi-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn suzi:app
    envVars:
      - key: OPENAI_API_KEY
        sync: false
